import styles from "./NotFoundWord.module.scss";

interface NotFoundWordProps {}

export const NotFoundWord = ({}: NotFoundWordProps) => (
  <div className={styles.notFoundWord}>NotFoundWord Component</div>
);
